import { notImplemented } from "./_notImplemented";

/** Finds client/payer credit. */
export const detectCreditBalance = (...args: unknown[]): never => notImplemented("detectCreditBalance", args);

/** Determines eligible refund amount. */
export const calculateRefundableAmount = (...args: unknown[]): never => notImplemented("calculateRefundableAmount", args);

/** Checks amount, source, reason. */
export const validateRefundRequest = (...args: unknown[]): never => notImplemented("validateRefundRequest", args);

/** Creates refund ledger payload. */
export const buildRefundLedgerTransaction = (...args: unknown[]): never => notImplemented("buildRefundLedgerTransaction", args);

/** Moves credit to another balance. */
export const buildCreditTransferPayload = (...args: unknown[]): never => notImplemented("buildCreditTransferPayload", args);

/** Displays refund status. */
export const formatRefundStatus = (...args: unknown[]): never => notImplemented("formatRefundStatus", args);

/** Displays credit status. */
export const formatCreditBalanceStatus = (...args: unknown[]): never => notImplemented("formatCreditBalanceStatus", args);

/** Identifies payer recoupment/offset. */
export const detectPayerRecoupment = (...args: unknown[]): never => notImplemented("detectPayerRecoupment", args);
