import { notImplemented } from "./_notImplemented";

/** Groups claims by payer/submission method. */
export const groupClaimsForBatch = (...args: unknown[]): never => notImplemented("groupClaimsForBatch", args);

/** Claim count and total charges. */
export const calculateBatchTotals = (...args: unknown[]): never => notImplemented("calculateBatchTotals", args);

/** Confirms all claims are batch-ready. */
export const validateBatchClaims = (...args: unknown[]): never => notImplemented("validateBatchClaims", args);

/** Creates consistent file name. */
export const build837PFilename = (...args: unknown[]): never => notImplemented("build837PFilename", args);

/** Prevents duplicate file submission. */
export const generateSubmissionChecksum = (...args: unknown[]): never => notImplemented("generateSubmissionChecksum", args);

/** Displays submission status. */
export const formatSubmissionStatus = (...args: unknown[]): never => notImplemented("formatSubmissionStatus", args);

/** Converts response into system status. */
export const mapClearinghouseResponse = (...args: unknown[]): never => notImplemented("mapClearinghouseResponse", args);

/** Converts 277 response into claim status. */
export const map277StatusResponse = (...args: unknown[]): never => notImplemented("map277StatusResponse", args);

/** Pulls payer claim/control number from response. */
export const extractPayerClaimNumber = (...args: unknown[]): never => notImplemented("extractPayerClaimNumber", args);
