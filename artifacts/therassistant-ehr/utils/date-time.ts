import { notImplemented } from "./_notImplemented";

/** Formats dates for display. */
export const formatDate = (...args: unknown[]): never => notImplemented("formatDate", args);

/** Formats timestamps. */
export const formatDateTime = (...args: unknown[]): never => notImplemented("formatDateTime", args);

/** Formats appointment start/end times. */
export const formatTime = (...args: unknown[]): never => notImplemented("formatTime", args);

/** Displays service date ranges. */
export const formatDateRange = (...args: unknown[]): never => notImplemented("formatDateRange", args);

/** Safely parses form date inputs. */
export const parseDateInput = (...args: unknown[]): never => notImplemented("parseDateInput", args);

/** Converts date to ISO format. */
export const toIsoDate = (...args: unknown[]): never => notImplemented("toIsoDate", args);

/** Converts UTC date to local display date. */
export const toLocalDate = (...args: unknown[]): never => notImplemented("toLocalDate", args);

/** Returns tenant/practice timezone. */
export const getTenantTimezone = (...args: unknown[]): never => notImplemented("getTenantTimezone", args);

/** Converts timestamps to practice timezone. */
export const convertToTenantTimezone = (...args: unknown[]): never => notImplemented("convertToTenantTimezone", args);

/** Calculates client age from DOB. */
export const calculateAge = (...args: unknown[]): never => notImplemented("calculateAge", args);

/** Checks if DOS falls within coverage/auth/contract dates. */
export const isDateInRange = (...args: unknown[]): never => notImplemented("isDateInRange", args);

/** Calculates days between two dates. */
export const daysBetween = (...args: unknown[]): never => notImplemented("daysBetween", args);

/** Calculates business days for deadlines. */
export const businessDaysBetween = (...args: unknown[]): never => notImplemented("businessDaysBetween", args);

/** Adds business days to deadline dates. */
export const addBusinessDays = (...args: unknown[]): never => notImplemented("addBusinessDays", args);

/** Checks if auth, license, treatment plan, or policy is expired. */
export const isExpired = (...args: unknown[]): never => notImplemented("isExpired", args);

/** Checks upcoming expiration thresholds. */
export const isExpiringSoon = (...args: unknown[]): never => notImplemented("isExpiringSoon", args);
