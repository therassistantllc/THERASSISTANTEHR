import { notImplemented } from "./_notImplemented";

/** Displays payment, credit, opening balance, etc. */
export const formatHistoricalTransactionType = (...args: unknown[]): never => notImplemented("formatHistoricalTransactionType", args);

/** Validates client, amount, type, date. */
export const validateHistoricalTransaction = (...args: unknown[]): never => notImplemented("validateHistoricalTransaction", args);

/** Posts historical transaction to ledger. */
export const buildHistoricalLedgerTransaction = (...args: unknown[]): never => notImplemented("buildHistoricalLedgerTransaction", args);

/** Checks whether linking to claim is allowed. */
export const canLinkHistoricalTransaction = (...args: unknown[]): never => notImplemented("canLinkHistoricalTransaction", args);

/** Links historical item to later claim. */
export const buildHistoricalLinkPayload = (...args: unknown[]): never => notImplemented("buildHistoricalLinkPayload", args);

/** Reverses historical transaction. */
export const buildHistoricalReversalPayload = (...args: unknown[]): never => notImplemented("buildHistoricalReversalPayload", args);

/** Converts imported type into system enum. */
export const mapLegacyTransactionType = (...args: unknown[]): never => notImplemented("mapLegacyTransactionType", args);

/** Cleans prior-system source name. */
export const normalizeLegacySourceName = (...args: unknown[]): never => notImplemented("normalizeLegacySourceName", args);
