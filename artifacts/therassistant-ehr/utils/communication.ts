import { notImplemented } from "./_notImplemented";

/** Formats phone numbers. */
export const formatPhoneNumber = (...args: unknown[]): never => notImplemented("formatPhoneNumber", args);

/** Stores/searches phone consistently. */
export const normalizePhoneNumber = (...args: unknown[]): never => notImplemented("normalizePhoneNumber", args);

/** Checks phone format. */
export const validatePhoneNumber = (...args: unknown[]): never => notImplemented("validatePhoneNumber", args);

/** Normalizes email display. */
export const formatEmail = (...args: unknown[]): never => notImplemented("formatEmail", args);

/** Validates email address. */
export const validateEmail = (...args: unknown[]): never => notImplemented("validateEmail", args);

/** Masks email for privacy. */
export const maskEmail = (...args: unknown[]): never => notImplemented("maskEmail", args);

/** Formats address blocks. */
export const formatAddress = (...args: unknown[]): never => notImplemented("formatAddress", args);

/** Checks required address fields. */
export const validateAddressFields = (...args: unknown[]): never => notImplemented("validateAddressFields", args);

/** Determines portal/email/phone/mail preference. */
export const getPreferredContactMethod = (...args: unknown[]): never => notImplemented("getPreferredContactMethod", args);

/** Displays inbound/outbound/internal. */
export const formatCommunicationDirection = (...args: unknown[]): never => notImplemented("formatCommunicationDirection", args);

/** Displays phone, email, fax, portal, etc. */
export const formatCommunicationType = (...args: unknown[]): never => notImplemented("formatCommunicationType", args);
