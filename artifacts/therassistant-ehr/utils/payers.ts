import { notImplemented } from "./_notImplemented";

/** Cleans payer name for matching. */
export const normalizePayerName = (...args: unknown[]): never => notImplemented("normalizePayerName", args);

/** Matches ERA/import payer names to payer record. */
export const matchPayerAlias = (...args: unknown[]): never => notImplemented("matchPayerAlias", args);

/** Finds contract active on DOS. */
export const getActiveContractForDos = (...args: unknown[]): never => notImplemented("getActiveContractForDos", args);

/** Finds fee schedule rate. */
export const getRateForCpt = (...args: unknown[]): never => notImplemented("getRateForCpt", args);

/** Calculates expected allowed amount. */
export const calculateExpectedAllowed = (...args: unknown[]): never => notImplemented("calculateExpectedAllowed", args);

/** Calculates expected payer payment. */
export const calculateExpectedPayment = (...args: unknown[]): never => notImplemented("calculateExpectedPayment", args);

/** Detects underpayment/overpayment. */
export const compareExpectedToActual = (...args: unknown[]): never => notImplemented("compareExpectedToActual", args);

/** Displays contract status. */
export const formatContractStatus = (...args: unknown[]): never => notImplemented("formatContractStatus", args);

/** Displays rate type. */
export const formatRateType = (...args: unknown[]): never => notImplemented("formatRateType", args);

/** Checks contract dates. */
export const isContractExpired = (...args: unknown[]): never => notImplemented("isContractExpired", args);

/** Identifies payment variance. */
export const detectContractVariance = (...args: unknown[]): never => notImplemented("detectContractVariance", args);
