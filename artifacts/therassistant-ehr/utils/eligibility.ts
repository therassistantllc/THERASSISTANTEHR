import { notImplemented } from "./_notImplemented";

/** Cleans/displays member ID. */
export const formatMemberId = (...args: unknown[]): never => notImplemented("formatMemberId", args);

/** Removes spaces/dashes for matching. */
export const normalizeMemberId = (...args: unknown[]): never => notImplemented("normalizeMemberId", args);

/** Displays group number. */
export const formatGroupNumber = (...args: unknown[]): never => notImplemented("formatGroupNumber", args);

/** Finds active policy for date of service. */
export const getActiveInsuranceForDos = (...args: unknown[]): never => notImplemented("getActiveInsuranceForDos", args);

/** Returns active primary policy. */
export const getPrimaryInsurance = (...args: unknown[]): never => notImplemented("getPrimaryInsurance", args);

/** Returns secondary policy. */
export const getSecondaryInsurance = (...args: unknown[]): never => notImplemented("getSecondaryInsurance", args);

/** Ensures effective/termination dates are valid. */
export const validateInsuranceDates = (...args: unknown[]): never => notImplemented("validateInsuranceDates", args);

/** Detects overlapping active policies. */
export const hasOverlappingCoverage = (...args: unknown[]): never => notImplemented("hasOverlappingCoverage", args);

/** Displays subscriber info. */
export const formatSubscriberName = (...args: unknown[]): never => notImplemented("formatSubscriberName", args);

/** Determines deductible remaining if data exists. */
export const calculateDeductibleRemaining = (...args: unknown[]): never => notImplemented("calculateDeductibleRemaining", args);

/** Determines out-of-pocket remaining if data exists. */
export const calculateOopRemaining = (...args: unknown[]): never => notImplemented("calculateOopRemaining", args);

/** Finds CPT-specific benefit. */
export const getBenefitForCpt = (...args: unknown[]): never => notImplemented("getBenefitForCpt", args);

/** Finds expected copay. */
export const getCopayForService = (...args: unknown[]): never => notImplemented("getCopayForService", args);

/** Finds expected coinsurance. */
export const getCoinsuranceForService = (...args: unknown[]): never => notImplemented("getCoinsuranceForService", args);

/** Checks auth requirement from benefit/payer rules. */
export const requiresAuthorization = (...args: unknown[]): never => notImplemented("requiresAuthorization", args);

/** Converts portal/import status into system enum. */
export const mapEligibilityStatus = (...args: unknown[]): never => notImplemented("mapEligibilityStatus", args);
