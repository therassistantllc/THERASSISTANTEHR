import { notImplemented } from "./_notImplemented";

/** Lowercase/trim/remove extra spaces. */
export const normalizeSearchQuery = (...args: unknown[]): never => notImplemented("normalizeSearchQuery", args);

/** Tokenizes search input. */
export const buildSearchTokens = (...args: unknown[]): never => notImplemented("buildSearchTokens", args);

/** Highlights matching text in UI. */
export const highlightSearchMatch = (...args: unknown[]): never => notImplemented("highlightSearchMatch", args);

/** Client search params. */
export const buildClientSearchQuery = (...args: unknown[]): never => notImplemented("buildClientSearchQuery", args);

/** Claim search params. */
export const buildClaimSearchQuery = (...args: unknown[]): never => notImplemented("buildClaimSearchQuery", args);

/** Payment search params. */
export const buildPaymentSearchQuery = (...args: unknown[]): never => notImplemented("buildPaymentSearchQuery", args);

/** Converts UI filters to API params. */
export const buildWorkqueueFilters = (...args: unknown[]): never => notImplemented("buildWorkqueueFilters", args);

/** Creates date range filter. */
export const buildDateRangeFilter = (...args: unknown[]): never => notImplemented("buildDateRangeFilter", args);

/** Creates status filter. */
export const buildStatusFilter = (...args: unknown[]): never => notImplemented("buildStatusFilter", args);

/** Page/limit/offset. */
export const buildPaginationParams = (...args: unknown[]): never => notImplemented("buildPaginationParams", args);

/** Sort field/direction. */
export const buildSortParams = (...args: unknown[]): never => notImplemented("buildSortParams", args);
