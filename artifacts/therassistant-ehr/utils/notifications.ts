import { notImplemented } from "./_notImplemented";

/** Creates notification record. */
export const buildNotificationPayload = (...args: unknown[]): never => notImplemented("buildNotificationPayload", args);

/** Displays notification type. */
export const formatNotificationType = (...args: unknown[]): never => notImplemented("formatNotificationType", args);

/** Checks preferences/routing. */
export const shouldSendNotification = (...args: unknown[]): never => notImplemented("shouldSendNotification", args);

/** Determines users/roles to notify. */
export const getNotificationRecipients = (...args: unknown[]): never => notImplemented("getNotificationRecipients", args);

/** Creates message text. */
export const buildNotificationMessage = (...args: unknown[]): never => notImplemented("buildNotificationMessage", args);

/** Prevents repeated alerts. */
export const dedupeNotification = (...args: unknown[]): never => notImplemented("dedupeNotification", args);

/** Displays timestamp. */
export const formatNotificationTimestamp = (...args: unknown[]): never => notImplemented("formatNotificationTimestamp", args);
