import { notImplemented } from "./_notImplemented";

/** Cleans CARC code. */
export const normalizeCarcCode = (...args: unknown[]): never => notImplemented("normalizeCarcCode", args);

/** Cleans RARC code. */
export const normalizeRarcCode = (...args: unknown[]): never => notImplemented("normalizeRarcCode", args);

/** Returns CARC description. */
export const getCarcDescription = (...args: unknown[]): never => notImplemented("getCarcDescription", args);

/** Returns RARC description. */
export const getRarcDescription = (...args: unknown[]): never => notImplemented("getRarcDescription", args);

/** Maps CARC to denial type. */
export const mapCarcToDenialCategory = (...args: unknown[]): never => notImplemented("mapCarcToDenialCategory", args);

/** Workable vs non-workable. */
export const determineDenialWorkability = (...args: unknown[]): never => notImplemented("determineDenialWorkability", args);

/** Flags credentialing denials. */
export const isCredentialingDenial = (...args: unknown[]): never => notImplemented("isCredentialingDenial", args);

/** Flags contract issues. */
export const isContractingDenial = (...args: unknown[]): never => notImplemented("isContractingDenial", args);

/** Detects deductible/copay/coinsurance PR. */
export const isclientResponsibilityCarc = (...args: unknown[]): never => notImplemented("isclientResponsibilityCarc", args);

/** Corrected claim, appeal, reconsideration, write-off, etc. */
export const suggestDenialNextAction = (...args: unknown[]): never => notImplemented("suggestDenialNextAction", args);

/** Calculates appeal deadline from payer/rule/date. */
export const calculateAppealDeadline = (...args: unknown[]): never => notImplemented("calculateAppealDeadline", args);

/** Displays denial reason clearly. */
export const formatDenialReason = (...args: unknown[]): never => notImplemented("formatDenialReason", args);
