import { notImplemented } from "./_notImplemented";

/** Detects changed form fields. */
export const getDirtyFields = (...args: unknown[]): never => notImplemented("getDirtyFields", args);

/** Dirty-state warning. */
export const hasUnsavedChanges = (...args: unknown[]): never => notImplemented("hasUnsavedChanges", args);

/** Resets form. */
export const resetFormToDefaults = (...args: unknown[]): never => notImplemented("resetFormToDefaults", args);

/** Converts form strings to typed values. */
export const coerceFormValues = (...args: unknown[]): never => notImplemented("coerceFormValues", args);

/** Sends only changed fields. */
export const buildPatchPayload = (...args: unknown[]): never => notImplemented("buildPatchPayload", args);

/** Builds insert payload. */
export const buildCreatePayload = (...args: unknown[]): never => notImplemented("buildCreatePayload", args);

/** Displays validation errors on fields. */
export const mapApiErrorsToFormErrors = (...args: unknown[]): never => notImplemented("mapApiErrorsToFormErrors", args);

/** UX helper. */
export const scrollToFirstError = (...args: unknown[]): never => notImplemented("scrollToFirstError", args);

/** Checks loading/permission/validation state. */
export const isSubmitDisabled = (...args: unknown[]): never => notImplemented("isSubmitDisabled", args);

/** Converts data to dropdown options. */
export const prepareSelectOptions = (...args: unknown[]): never => notImplemented("prepareSelectOptions", args);
