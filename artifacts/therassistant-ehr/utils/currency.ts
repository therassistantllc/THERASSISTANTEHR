import { notImplemented } from "./_notImplemented";

/** Displays dollar amounts. */
export const formatCurrency = (...args: unknown[]): never => notImplemented("formatCurrency", args);

/** Converts form currency input to decimal/cents. */
export const parseCurrencyInput = (...args: unknown[]): never => notImplemented("parseCurrencyInput", args);

/** Converts dollars to integer cents. */
export const toCents = (...args: unknown[]): never => notImplemented("toCents", args);

/** Converts cents to dollars. */
export const fromCents = (...args: unknown[]): never => notImplemented("fromCents", args);

/** Rounds money safely. */
export const roundCurrency = (...args: unknown[]): never => notImplemented("roundCurrency", args);

/** Adds currency values safely. */
export const sumCurrency = (...args: unknown[]): never => notImplemented("sumCurrency", args);

/** Subtracts amounts safely. */
export const subtractCurrency = (...args: unknown[]): never => notImplemented("subtractCurrency", args);

/** Checks whether balance equals zero. */
export const isZeroBalance = (...args: unknown[]): never => notImplemented("isZeroBalance", args);

/** Checks whether balance is negative/credit. */
export const isCreditBalance = (...args: unknown[]): never => notImplemented("isCreditBalance", args);

/** Detects payment greater than open balance. */
export const isOverpayment = (...args: unknown[]): never => notImplemented("isOverpayment", args);

/** Charges minus payments minus adjustments. */
export const calculateOpenBalance = (...args: unknown[]): never => notImplemented("calculateOpenBalance", args);

/** Calculates allowed amount from contract/fee schedule. */
export const calculateAllowedAmount = (...args: unknown[]): never => notImplemented("calculateAllowedAmount", args);

/** Expected vs actual reimbursement. */
export const calculateVariance = (...args: unknown[]): never => notImplemented("calculateVariance", args);

/** Calculates rate/percentage metrics */
export const calculatePercent = (...args: unknown[]): never => notImplemented("calculatePercent", args);
