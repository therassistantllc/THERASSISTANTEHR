import { notImplemented } from "./_notImplemented";

/** Converts ERA date formats. */
export const parse835Date = (...args: unknown[]): never => notImplemented("parse835Date", args);

/** Standardizes payer names. */
export const normalizeEraPayerName = (...args: unknown[]): never => notImplemented("normalizeEraPayerName", args);

/** Standardizes payer claim number. */
export const normalizeEraClaimNumber = (...args: unknown[]): never => notImplemented("normalizeEraClaimNumber", args);

/** Matches by claim number/control/DOS/member. */
export const matchEraClaimToInternalClaim = (...args: unknown[]): never => notImplemented("matchEraClaimToInternalClaim", args);

/** Matches CPT/DOS/amount to claim line. */
export const matchEraServiceLine = (...args: unknown[]): never => notImplemented("matchEraServiceLine", args);

/** Sums ERA payments. */
export const calculateEraPaymentTotal = (...args: unknown[]): never => notImplemented("calculateEraPaymentTotal", args);

/** Sums adjustments. */
export const calculateEraAdjustmentTotal = (...args: unknown[]): never => notImplemented("calculateEraAdjustmentTotal", args);

/** Converts ERA status to system status. */
export const mapEraClaimStatus = (...args: unknown[]): never => notImplemented("mapEraClaimStatus", args);

/** Pulls CARC codes. */
export const extractCarcCodes = (...args: unknown[]): never => notImplemented("extractCarcCodes", args);

/** Pulls RARC codes. */
export const extractRarcCodes = (...args: unknown[]): never => notImplemented("extractRarcCodes", args);

/** Prevents duplicate ERA import/posting. */
export const detectEraDuplicate = (...args: unknown[]): never => notImplemented("detectEraDuplicate", args);

/** Shows what will post before posting. */
export const buildEraPostingPreview = (...args: unknown[]): never => notImplemented("buildEraPostingPreview", args);
