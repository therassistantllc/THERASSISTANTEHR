import { notImplemented } from "./_notImplemented";

/** Calculates copay/deductible/coinsurance/self-pay. */
export const calculateclientResponsibility = (...args: unknown[]): never => notImplemented("calculateclientResponsibility", args);

/** Displays PR type. */
export const formatclientResponsibilityType = (...args: unknown[]): never => notImplemented("formatclientResponsibilityType", args);

/** Converts open PR items to statement lines. */
export const buildStatementLines = (...args: unknown[]): never => notImplemented("buildStatementLines", args);

/** Sums statement lines. */
export const calculateStatementTotal = (...args: unknown[]): never => notImplemented("calculateStatementTotal", args);

/** Displays statement date range. */
export const formatStatementPeriod = (...args: unknown[]): never => notImplemented("formatStatementPeriod", args);

/** Checks due date. */
export const isStatementPastDue = (...args: unknown[]): never => notImplemented("isStatementPastDue", args);

/** Generates installment schedule. */
export const calculatePaymentPlanSchedule = (...args: unknown[]): never => notImplemented("calculatePaymentPlanSchedule", args);

/** Divides balance across installments. */
export const calculateInstallmentAmount = (...args: unknown[]): never => notImplemented("calculateInstallmentAmount", args);

/** Current, past due, hardship, collections, written off. */
export const getCollectionStatus = (...args: unknown[]): never => notImplemented("getCollectionStatus", args);

/** Determines if statement should be created. */
export const shouldGenerateStatement = (...args: unknown[]): never => notImplemented("shouldGenerateStatement", args);
