import { notImplemented } from "./_notImplemented";

/** Displays provider name/credentials. */
export const formatProviderName = (...args: unknown[]): never => notImplemented("formatProviderName", args);

/** LPC, LCSW, LAC, PMHNP, etc. */
export const formatProviderCredentials = (...args: unknown[]): never => notImplemented("formatProviderCredentials", args);

/** Validates NPI format/checksum. */
export const validateNpi = (...args: unknown[]): never => notImplemented("validateNpi", args);

/** Displays NPI. */
export const formatNpi = (...args: unknown[]): never => notImplemented("formatNpi", args);

/** Checks taxonomy format. */
export const validateTaxonomyCode = (...args: unknown[]): never => notImplemented("validateTaxonomyCode", args);

/** Checks license expiration. */
export const isProviderLicenseExpired = (...args: unknown[]): never => notImplemented("isProviderLicenseExpired", args);

/** Checks payer enrollment by DOS. */
export const isProviderEnrollmentActiveForDos = (...args: unknown[]): never => notImplemented("isProviderEnrollmentActiveForDos", args);

/** Credentialing/contract billing eligibility. */
export const canProviderBillPayer = (...args: unknown[]): never => notImplemented("canProviderBillPayer", args);

/** Returns rendering provider for claim. */
export const getRenderingProvider = (...args: unknown[]): never => notImplemented("getRenderingProvider", args);

/** Returns billing provider. */
export const getBillingProvider = (...args: unknown[]): never => notImplemented("getBillingProvider", args);

/** Returns supervising provider if needed. */
export const getSupervisingProvider = (...args: unknown[]): never => notImplemented("getSupervisingProvider", args);

/** Flags enrollment/contract problem. */
export const detectCredentialingIssue = (...args: unknown[]): never => notImplemented("detectCredentialingIssue", args);
