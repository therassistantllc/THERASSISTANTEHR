import { notImplemented } from "./_notImplemented";

/** Displays payment status. */
export const formatPaymentStatus = (...args: unknown[]): never => notImplemented("formatPaymentStatus", args);

/** Sums allocations. */
export const calculatePaymentAppliedAmount = (...args: unknown[]): never => notImplemented("calculatePaymentAppliedAmount", args);

/** Payment amount minus applied amount. */
export const calculatePaymentUnappliedAmount = (...args: unknown[]): never => notImplemented("calculatePaymentUnappliedAmount", args);

/** Checks if payment is fully allocated. */
export const isPaymentFullyApplied = (...args: unknown[]): never => notImplemented("isPaymentFullyApplied", args);

/** Prevents over-allocation. */
export const validatePaymentAllocation = (...args: unknown[]): never => notImplemented("validatePaymentAllocation", args);

/** Calculates allocation payload. */
export const allocatePaymentToClaim = (...args: unknown[]): never => notImplemented("allocatePaymentToClaim", args);

/** Service-line allocation helper. */
export const allocatePaymentToClaimLine = (...args: unknown[]): never => notImplemented("allocatePaymentToClaimLine", args);

/** Checks payer/check/trace/date/amount. */
export const detectDuplicatePayment = (...args: unknown[]): never => notImplemented("detectDuplicatePayment", args);

/** Detects overpayment. */
export const detectPaymentOverage = (...args: unknown[]): never => notImplemented("detectPaymentOverage", args);

/** Creates ledger transaction payload. */
export const buildPaymentLedgerTransaction = (...args: unknown[]): never => notImplemented("buildPaymentLedgerTransaction", args);

/** Creates reversal payload. */
export const buildPaymentReversalPayload = (...args: unknown[]): never => notImplemented("buildPaymentReversalPayload", args);

/** Displays EFT/check reference. */
export const formatCheckOrTraceNumber = (...args: unknown[]): never => notImplemented("formatCheckOrTraceNumber", args);
