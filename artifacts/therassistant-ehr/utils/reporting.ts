import { notImplemented } from "./_notImplemented";

/** Clean claims / total claims. */
export const calculateCleanClaimRate = (...args: unknown[]): never => notImplemented("calculateCleanClaimRate", args);

/** Paid on first pass / total claims. */
export const calculateFirstPassYield = (...args: unknown[]): never => notImplemented("calculateFirstPassYield", args);

/** Average AR age. */
export const calculateDaysInAr = (...args: unknown[]): never => notImplemented("calculateDaysInAr", args);

/** 0–30, 31–60, 61–90, 91+. */
export const calculateArAgingBucket = (...args: unknown[]): never => notImplemented("calculateArAgingBucket", args);

/** Payments / expected allowed. */
export const calculateNetCollectionRate = (...args: unknown[]): never => notImplemented("calculateNetCollectionRate", args);

/** Payments / charges. */
export const calculateGrossCollectionRate = (...args: unknown[]): never => notImplemented("calculateGrossCollectionRate", args);

/** Denials / submitted claims. */
export const calculateDenialRate = (...args: unknown[]): never => notImplemented("calculateDenialRate", args);

/** client payments / client responsibility. */
export const calculateclientCollectionRate = (...args: unknown[]): never => notImplemented("calculateclientCollectionRate", args);

/** Payment average by visit/CPT/provider. */
export const calculateAverageReimbursement = (...args: unknown[]): never => notImplemented("calculateAverageReimbursement", args);

/** Payer share by visits/charges/payments. */
export const calculatePayerMix = (...args: unknown[]): never => notImplemented("calculatePayerMix", args);

/** Auth-required claims with valid auth. */
export const calculateAuthHitRate = (...args: unknown[]): never => notImplemented("calculateAuthHitRate", args);

/** Display report period. */
export const formatReportDateRange = (...args: unknown[]): never => notImplemented("formatReportDateRange", args);

/** Report grouping. */
export const groupByPayer = (...args: unknown[]): never => notImplemented("groupByPayer", args);

/** Report grouping. */
export const groupByProvider = (...args: unknown[]): never => notImplemented("groupByProvider", args);

/** Report grouping. */
export const groupByCpt = (...args: unknown[]): never => notImplemented("groupByCpt", args);

/** CSV export helper. */
export const exportToCsv = (...args: unknown[]): never => notImplemented("exportToCsv", args);
