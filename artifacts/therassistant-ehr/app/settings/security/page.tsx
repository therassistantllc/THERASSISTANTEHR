import { SettingsPlaceholderTable } from "../_components/SettingsTables";

export default function SecuritySettingsPage() {
  return (
    <SettingsPlaceholderTable
      title="Security"
      route="/settings/security"
      purpose="Security settings route is available for setup."
      status="Placeholder"
    />
  );
}
