import { SettingsPlaceholderTable } from "../_components/SettingsTables";

export default function EDISettingsPage() {
  return (
    <SettingsPlaceholderTable
      title="EDI Setup"
      route="/settings/edi"
      purpose="EDI setup route is available for configuration planning."
      status="Setup needed"
    />
  );
}
