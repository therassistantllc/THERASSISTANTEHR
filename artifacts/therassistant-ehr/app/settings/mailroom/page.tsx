import { SettingsPlaceholderTable } from "../_components/SettingsTables";

export default function MailroomSettingsPage() {
  return (
    <SettingsPlaceholderTable
      title="Mailroom Settings"
      route="/settings/mailroom"
      purpose="Mailroom settings route is available for setup."
      status="Placeholder"
    />
  );
}
