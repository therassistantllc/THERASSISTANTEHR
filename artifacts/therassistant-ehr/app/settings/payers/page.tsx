import { SettingsPlaceholderTable } from "../_components/SettingsTables";

export default function PayersSettingsPage() {
  return (
    <SettingsPlaceholderTable
      title="Payers"
      route="/settings/payers"
      purpose="Payer profile settings route is available for setup."
      status="Placeholder"
    />
  );
}
