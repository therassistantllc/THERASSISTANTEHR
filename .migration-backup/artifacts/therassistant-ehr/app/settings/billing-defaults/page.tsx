import { SettingsPlaceholderTable } from "../_components/SettingsTables";

export default function BillingDefaultsSettingsPage() {
  return (
    <SettingsPlaceholderTable
      title="Billing Defaults"
      route="/settings/billing-defaults"
      purpose="Billing defaults settings route is available for setup."
      status="Placeholder"
    />
  );
}
